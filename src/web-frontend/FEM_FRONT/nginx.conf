server {
    listen 8050;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: disable .htaccess files
    location ~ /\.ht {
        deny all;
    }
}
