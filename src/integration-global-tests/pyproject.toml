[project]
name = "integration-global-tests"
version = "0.1.0"
description = "FEM's Global Integration Tests"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncio>=4.0.0",
    "httpx>=0.28.1",
    "pytest>=9.0.2",
]
